#!/bin/bash

HUGGINGFACE_EMAIL=$1
HUGGINGFACE_PASSWORD=$2
ENV_FILE_PATH="$HOME/.config/chat-sotero/.env"
ENVPATH="$HOME/.config/chat-sotero/"
mkdir -p $ENVPATH

cat > $ENV_FILE_PATH <<EOF
HUGGINGFACE_EMAIL=$HUGGINGFACE_EMAIL
HUGGINGFACE_PASSWORD=$HUGGINGFACE_PASSWORD
EOF
chmod 600 $ENV_FILE_PATH
